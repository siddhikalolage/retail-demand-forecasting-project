# packages.yml — external dbt package dependencies.
# Run `dbt deps` after editing to fetch into dbt_packages/.
# See DBT_PIPELINE.md for the full walkthrough.

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]
